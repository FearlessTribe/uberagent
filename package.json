{
  "name": "uberagent",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix website && npm run build --prefix website",
    "dev": "npm run dev --prefix website"
  },
  "engines": {
    "node": ">=20"
  }
}
